# AI service tiers: demo (BGE + LM Studio) vs enterprise (vLLM + Qwen-class).
# See docs/AI_SERVICE_TIERS.md and docs/LOCAL_AI_CONTOUR.md.

app:
  name: Bankable Property OS
  demo_case_id: case-anchor-deposit-mismatch

rag:
  qdrant_url: ${QDRANT_URL}
  collection_prefix: ${QDRANT_COLLECTION_PREFIX}
  collections:
    - policies
    - developers
    - risk_cases
    - settlement_rules

services:
  embedding_base_url: ${LOCAL_AI_EMBEDDING_BASE_URL}
  reranker_base_url: ${LOCAL_AI_RERANKER_BASE_URL}
  llm_instruct_base_url: ${LOCAL_AI_LLM_INSTRUCT_BASE_URL}
